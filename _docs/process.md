# Development Process

GitHub Issues are the canonical active backlog. `_docs/tasks.md` is the original versioned backlog snapshot and is not the active task tracker.

## Issue workflow

1. Work on one GitHub Issue at a time unless explicitly instructed otherwise.
2. For a non-trivial Issue, begin with a fresh PM/grooming session. Read the Issue, `_docs/plan.md`, and the context documents relevant to the change, including the current architecture document when applicable.
3. The PM produces or refines:
   - Goal
   - Acceptance Criteria
   - Out of Scope
   - Constraints
   - Deferred Decisions, when relevant
4. Give the specification a version-identifiable revision. The user must approve that revision before implementation; a Software Engineer must not rewrite or silently reinterpret approved Acceptance Criteria.
5. A fresh Software Engineer session implements only the approved Issue on a feature branch and submits a pull request. Future non-trivial implementation work must not be pushed directly to `main`.
6. The Engineer runs the four root gates (`format:check`, `lint`, `typecheck`, and `test`), reviews the diff, commits completed work with a meaningful message, and records the candidate commit in the PR.
7. CI and a fresh independent QA session verify the identified candidate. QA reports findings and does not repair the implementation. A fresh QA session provides sufficient independence; a second human reviewer is not required, and the solo developer remains the decision-maker.
8. QA evidence identifies the Issue and approved specification revision, candidate and base commits, commit actually tested, CI run and relevant verification environment, and evidence for every Acceptance Criterion. Prefer existing GitHub metadata and links instead of a separate evidence system. Identify mock-based checks as mocks.
9. Use `PASS` only when every required criterion is verified, `FAIL` when at least one criterion is demonstrably unmet, and `BLOCKED / NOT VERIFIED` when required evidence is unavailable. Missing required evidence cannot produce an overall `PASS`.
10. After an initial QA failure, permit at most two repair attempts. Each attempt returns to a fresh Software Engineer session for the smallest correction and then to independent QA. Stop earlier for conflicting specifications, required scope changes, missing authorization, or an unavailable required environment.
11. Any changed candidate requires renewed relevant verification and approval: rerun all four root gates, retest affected criteria and regressions, and account for every criterion in the new verdict. Relevant unchanged evidence may be referenced rather than duplicated.
12. The user must explicitly approve the exact candidate before merge. After merge, verify that the approved change reached `main`, record the reviewed commit and resulting merge information, and distinguish the feature commit from any newly created merge commit. Only then may the Issue be closed.
13. Do not start the next Issue automatically.
14. When a recurring correction reveals a reusable rule, update the appropriate living document in a separate change.

Very small repository-maintenance or documentation-only tasks may skip formal grooming when the requested change is already unambiguous. Fresh sessions are preferred between PM, Software Engineer, and QA to preserve context independence. The user remains the orchestrator for now.

Do not introduce automated orchestration, parallel agents, worktrees, or reusable Skills yet. Introduce them only after this workflow has been exercised enough to justify automation. The narrowly approved native pilot setup below is the only current exception; it does not authorize unattended development.

Product-scope or architecture changes, cloud-resource changes, deployments or other production actions, and repository-permission changes require explicit user approval. Branch protection or rulesets may reinforce this process, but changing repository settings is a separately approved action.

## Opt-in supervised Issue #4 pilot

Setup revision P1, approval `P1-setup-approved-with-clarifications-v1`, authorizes only the six-file configuration delivery (`.codex/config.toml`, the three `.codex/agents/pilot_*.toml` files, `AGENTS.md`, and this document) and bounded synthetic non-product tests. It does not authorize grooming or implementing live Issue #4, merging the setup PR, or activating unattended work. Capability availability and role descriptions are not invocation authority. Setup PASS and configuration merge do not start Issue #4; the user must later explicitly activate that one pilot. Independent setup QA and exact-candidate user merge approval remain separate gates.

The user's subsequent narrow amendment to PR #86 permits one repository-scoped skill, `.agents/skills/develop-issue/SKILL.md`, with explicit-invocation policy in its `agents/openai.yaml`, and the necessary routing and sequential-handoff documentation changes. `$develop-issue` is an opt-in entry point for the coordinating main session using the existing roles and this process; it does not create another team or authorize broad automatic task selection. Require an explicitly selected Issue and user activation before any product handoff. The amendment authorizes skill delivery only, not product invocation, live Issue #4 work, or full P1 acceptance.

The user selects GPT-6 Astra / Medium for the coordinator session; do not set project-wide model or reasoning defaults. The native roles are `pilot_pm`, `pilot_swe`, and `pilot_qa`, each explicitly configured with `gpt-5.6-sol` and `medium`. Use only those named roles after opt-in, sequentially, keeping `agents.max_concurrent_threads_per_session = 1` unchanged. Wait for native completion confirming that the previous role is no longer executing before attempting the next role. A final answer alone is not proof of thread closure. Explicit close/release is not a prerequisite for the next attempt; record any supported release result if available, without inventing controls or using interrupt as closure. Stop as BLOCKED on an actual invocation failure or if completion cannot be established; do not retry by raising the cap, substituting roles, or switching clients. Each handoff starts a fresh context without any parent conversation history (use the runtime's explicit no-fork option). Pass only the selected Issue or synthetic case, relevant source references, specification/approval evidence, and checkpoint. If named-role selection or fresh contexts are unavailable, report BLOCKED.

Observed evidence remains bounded to `P1-lifecycle-diagnostic-v1` on candidate `fd8b2d4e2a42be692d84f6063d19e4e65bd7f46c`, base `8fb80d9bf64a0ca3672a6fe10e77805970bd5539`: native `pilot_pm` (`/root/p1_lifecycle_pm`) completed before native `pilot_qa` (`/root/p1_lifecycle_qa`) was accepted and completed, both with `fork_turns="none"` and unchanged configuration. PM remained listed immediately after completion and was absent from the inventory after QA completed. No close/release operation was exposed or used. QA verified only the configured file value of 1. This demonstrates one accepted sequential handoff; neither the reclamation mechanism nor cap enforcement was proven. The native schema advertises the roles' model/effort, not evidence of their effective execution settings.

Preserve the original PR #86 configuration evidence and conversation checkpoints as historical evidence for their identified candidates. Original setup baseline: 0 named-role calls, 0 synthetic cases; the separately authorized diagnostic used 2 of 2 calls and completed two synthetic tasks (cumulative named-role calls: 2). Setup correction/retest cycles remain 0 of 2; the no-change diagnostic and this approved skill amendment do not reset the allowance or constitute a runtime correction/retest cycle. No additional role call is authorized by this amendment. Active runtime attribution remains uncertain: installed Desktop 26.908.40834 (8881) and bundled binary 0.154.0-alpha.6.2 do not prove the active runtime; shell metadata reported VS Code and process inspection was denied. Effective child model/effort/permissions, actual context isolation, cap enforcement/reclamation, disabled child delegation, `pilot_swe` invocation, missing-approval behavior, FAIL versus BLOCKED scenarios, stale candidate/base/specification handling, exhausted/resumed repairs, and harmless-target permission enforcement remain NOT VERIFIED. Skill discovery and application of explicit-only policy also require runtime evidence. New skill files do not establish full P1 acceptance, independent setup QA, or merge approval.

After future activation, PM proposes a version-identifiable specification and stops for explicit user approval; only then may SWE implement on a feature branch and submit an unmerged PR. Fresh independent QA inspects actual candidate artifacts, CI/logs, and relevant compilation evidence against every criterion using the Issue workflow above. SWE's completion claims cannot establish PASS. PM and QA must not write files or make remote mutations, including through shell, GitHub, apps, browser, or MCP. Read-only QA must not install dependencies or assume tests/builds can write outputs: use available evidence or an already authorized execution environment. Missing required evidence is BLOCKED, never an inferred PASS or permission escalation. Preserve demonstrated FAIL findings alongside any BLOCKED evidence gaps.

The coordinator preserves a checkpoint in the existing conversation/PR evidence: selected Issue/case; specification revision and explicit approval reference; candidate/base/tested SHAs; CI/environment and criterion evidence; QA verdict; repairs used (0–2), attempt number and in-progress/completed state; pending action; and any exact-candidate merge approval. Charge each repair attempt before work starts. At most two repairs follow the initial QA failure; interruptions, resumption, changed SHAs/specifications, or a new PR never reset the count. Resume only the recorded in-progress attempt; the second repair still receives fresh QA, but a third repair is BLOCKED. Missing or ambiguous state, conflicting scope, or unavailable permissions/environment stops the handoff as BLOCKED. Reconstruct state from recorded evidence before resuming; do not guess a zero count.

Changed candidates require all four root gates again and renewed relevant QA. Changed bases or specifications also invalidate prior readiness and merge approval: reassess affected criteria/regressions, account for every criterion, and obtain applicable renewed specification approval. Only evidence explicitly shown to remain applicable may be reused. User merge approval must bind the exact candidate, base, and approved specification; recheck all three immediately before any separately authorized merge. Pilot roles and the coordinator never automatically merge, close an Issue, or choose the next Issue; stop for the user's next instruction even after PASS.

`sandbox_mode` requests a runtime filesystem boundary; `[agents]` requests the one-open-child cap and disables child delegation. These are configuration controls whose effective Desktop behavior must be verified, not promises of enforcement. Live parent overrides and managed policies may affect effective permissions. Remote-write prohibitions, opt-in routing, fresh-context selection, approval gates, and repair accounting are instruction-only rules; a filesystem sandbox does not constrain every hosted connector. Never weaken sandbox/managed policy or user-level configuration to make setup or tests pass. A conflicting effective boundary is BLOCKED.

For setup acceptance, record Desktop and bundled-runtime versions, TOML parsing and supported-field validation separately from the four root gates. In the actual target Desktop runtime, verify named-role discovery/invocation, distinct contexts without history forks, effective model/effort/permissions, sequential completion and subsequent invocation under the unchanged cap, effective one-open-child enforcement, and disabled child delegation. Record reclamation or closure as NOT VERIFIED unless directly established; the observed handoff does not prove either. Use synthetic specifications and designated disposable non-product fixtures to check missing approvals, FAIL versus BLOCKED, stale candidate/base/specification approval, and exhausted/resumed repair checkpoints. Permission probes may touch only a designated harmless temporary target; retain actual sandbox denial evidence and never test remote GitHub mutations. Label scenario-only simulations separately from runtime tests and never request hidden reasoning. Limit setup tests to one initial pass and at most two targeted correction/retest cycles, preserving their count at resumption separately from the future Issue repair budget. Stop on unresolved required capability gaps; absence of an explicit close/release control alone is handled by the sequential attempt rule above. If this session cannot load the configuration, record the exact checkpoint in a draft PR for a fresh Desktop session or user restart; runtime acceptance stays pending. Do not change clients or invoke default agents as a workaround.

Developer Tools compilation remains a later Issue #4 acceptance gate. Resolve and document any missing compilation route before the relevant Issue acceptance; this setup does not authorize installing, configuring, or implementing the Mini Program. No framework, service, tracker, worktree, background job, additional Skill beyond `develop-issue`, hook system, adaptive model router, cloud action, or repository-setting change is included in this exception.
